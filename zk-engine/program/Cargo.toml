[package]
version = "0.1.0"
name = "sovereign-agent-program"
edition = "2021"

[dependencies]
sp1-zkvm = "3.0.0"          # The core zkVM guest library
serde = { version = "1.0", features = ["derive"] }
